// Root build file for the GeoTag Android SDK.
//
// NOTE: this module has never been compiled — the machine it was authored on has no JDK,
// no Gradle and no Android SDK. See android-sdk/README.md.
//
// Version pins (see README "Pinned versions" for confidence notes):
//   Android Gradle Plugin 8.5.2   — requires JDK 17 and Gradle 8.7+
//   Kotlin                2.0.21
plugins {
    id 'com.android.library' version '8.5.2' apply false
    id 'com.android.application' version '8.5.2' apply false
    id 'org.jetbrains.kotlin.android' version '2.0.21' apply false
}

tasks.register('clean', Delete) {
    delete rootProject.layout.buildDirectory
}
